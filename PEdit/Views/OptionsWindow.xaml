<Window x:Class="PEdit.Views.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Options" Height="280" Width="450"
        Background="#252526" Foreground="#D4D4D4" WindowStartupLocation="CenterOwner"
        Icon="/Assets/pedit-scuro.ico" >

    <Window.Resources>
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Border Background="#3E3E42" BorderThickness="0" CornerRadius="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                    </Grid.ColumnDefinitions>
                    <Path Grid.Column="1" Fill="#D4D4D4" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M0,0 L4,4 8,0 z"/>
                </Grid>
            </Border>
        </ControlTemplate>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Template="{StaticResource ComboBoxToggleButton}" Grid.Column="2" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press"/>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="5,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#2D2D30" BorderThickness="1" BorderBrush="#3E3E42"/>
                                    <ScrollViewer Margin="1" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#D4D4D4"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Font Family:" Margin="0,10,10,10" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" ItemsSource="{Binding SystemFonts}" SelectedItem="{Binding Config.FontFamily}" Margin="0,5" Padding="5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Font Size:" Margin="0,10,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Config.FontSize, UpdateSourceTrigger=PropertyChanged}" PreviewTextInput="NumericOnly" Background="#3E3E42" Foreground="White" BorderThickness="0" Padding="5" Margin="0,5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Background Color:" Margin="0,10,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Config.BackgroundColor}" IsReadOnly="True" Background="#3E3E42" Foreground="White" BorderThickness="0" Padding="5" Margin="0,5"/>
        <Button Grid.Row="2" Grid.Column="2" Content="..." Command="{Binding PickBgColorCommand}" Width="30" Margin="5,5,0,5" Background="#3E3E42" Foreground="White" BorderThickness="0"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Text Color:" Margin="0,10,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Config.ForegroundColor}" IsReadOnly="True" Background="#3E3E42" Foreground="White" BorderThickness="0" Padding="5" Margin="0,5"/>
        <Button Grid.Row="3" Grid.Column="2" Content="..." Command="{Binding PickFgColorCommand}" Width="30" Margin="5,5,0,5" Background="#3E3E42" Foreground="White" BorderThickness="0"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Selection Mode:" Margin="0,10,10,10" VerticalAlignment="Center"/>
        <CheckBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Content="Select entire word ignoring hyphens on double click" IsChecked="{Binding Config.SelectWholeWordOnDoubleClick}" Foreground="#D4D4D4" VerticalAlignment="Center" Margin="0,5"/>

        <Button Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" Content="Close" Command="{Binding CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}" Background="#007ACC" Foreground="White" BorderThickness="0" Padding="10,5" HorizontalAlignment="Right"/>
    </Grid>
</Window>